-- [[ 1. THE PERSISTENCE ENGINE ]]
local raw_url = "https://raw.githubusercontent.com/buckyport/YTP/refs/heads/main/YTP"
local queue_on_teleport = (queue_on_teleport or (syn and syn.queue_on_teleport) or (fluxus and fluxus.queue_on_teleport))

if queue_on_teleport then
    pcall(function()
        -- Queues this exact script to run again on the next server
        queue_on_teleport('loadstring(game:HttpGet("' .. raw_url .. '"))()')
    end)
end

if not game:IsLoaded() then game.Loaded:Wait() end

local player = game.Players.LocalPlayer
local pGui = player:WaitForChild("PlayerGui")
local VIM = game:GetService("VirtualInputManager")
local TeleportService = game:GetService("TeleportService")
local RS = game:GetService("RunService")
local camera = workspace.CurrentCamera

-- [[ 2. INFINITE CLICK CHECK (Menu Bypass) ]]
local function clearMenus()
    print("F9 LOG: Starting Menu Presence Check...")
    
    local title = pGui:WaitForChild("TitleScreen", 25)
    
    -- Keep checking/clicking until TitleScreen is no longer a child of PlayerGui
    while title and title.Parent == pGui do
        local btn = title:FindFirstChild("PlayButton")
        
        if btn and btn.Visible then
            local pos = btn.AbsolutePosition
            local size = btn.AbsoluteSize
            local clickX, clickY = pos.X + (size.X/2), pos.Y + (size.Y/2) + 36
            
            -- Press and release
            VIM:SendMouseButtonEvent(clickX, clickY, 0, true, game, 1)
            task.wait(0.05)
            VIM:SendMouseButtonEvent(clickX, clickY, 0, false, game, 1)
            
            print("F9 LOG: PlayButton detected and clicked. Checking for menu removal...")
        end
        
        task.wait(0.3) -- Faster menu polling
    end
    
    print("F9 LOG: Menu cleared from screen. Transitioning to game...")
end

-- [[ 3. TURBO SERVER HOP ]]
local function serverHop()
    print("F9 LOG: Collection complete or timeout hit. Entering 3s Hop Loop...")
    
    -- MODIFIED: Tries to hop every 3 seconds until it works
    while task.wait(3) do
        local success, result = pcall(function()
            return game:HttpGet("https://games.roblox.com/v1/games/" .. game.PlaceId .. "/servers/Public?sortOrder=Desc&limit=100")
        end)
        
        if success and result then
            local decoded = game:GetService("HttpService"):JSONDecode(result)
            if decoded and decoded.data then
                local possibleServers = {}
                for _, v in ipairs(decoded.data) do
                    if v.playing and v.maxPlayers and v.playing < v.maxPlayers and v.id ~= game.JobId then
                        table.insert(possibleServers, v.id)
                    end
                end
                
                if #possibleServers > 0 then
                    local target = possibleServers[math.random(1, #possibleServers)]
                    print("F9 LOG: Attempting teleport to Server: " .. target)
                    TeleportService:TeleportToPlaceInstance(game.PlaceId, target)
                else
                    TeleportService:Teleport(game.PlaceId, player)
                end
            end
        else
            TeleportService:Teleport(game.PlaceId, player)
        end
    end
end

-- [[ 4. THE CLEAN SWEEP ENGINE ]]
local function runTurbo()
    clearMenus()

    local char = player.Character or player.CharacterAdded:Wait()
    local root = char:WaitForChild("HumanoidRootPart", 20)

    -- Tracking for teleport frequency
    local lastTeleportTick = tick()

    -- Watchdog for teleport timeout
    task.spawn(function()
        while task.wait(0.5) do
            if (tick() - lastTeleportTick) > 3 then
                warn("F9 WARNING: No teleport for 3s. Hopping.")
                serverHop()
                break
            end
        end
    end)

    -- MODIFIED: Added Re-check logic
    local function performScan()
        local found = {}
        for _, v in pairs(workspace:GetDescendants()) do
            if (v.Name:find("YTP") or v.Name:find("YTP_")) and (v:IsA("Model") or v:IsA("BasePart")) then
                table.insert(found, v)
            end
        end
        return found
    end

    local items = performScan()
    if #items == 0 then serverHop() return end

    -- Main Collection Loop
    local function collectAll(list)
        for _, item in ipairs(list) do
            local hitbox = item:FindFirstChild("InteractionHitbox") or item:FindFirstChildWhichIsA("BasePart", true) or (item:IsA("BasePart") and item)
            
            if hitbox and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                local hrp = player.Character.HumanoidRootPart
                lastTeleportTick = tick()
                
                hrp.CFrame = hitbox.CFrame * CFrame.new(0, 0, 2.5) 
                hrp.Anchored = true
                
                local camLock = RS.RenderStepped:Connect(function()
                    if hitbox and hitbox.Parent then
                        camera.CFrame = CFrame.lookAt(camera.CFrame.Position, hitbox.Position)
                    end
                end)
                
                local clickStart = tick()
                while item.Parent and (tick() - clickStart) < 1.5 do
                    local cd = item:FindFirstChildWhichIsA("ClickDetector", true)
                    if cd then fireclickdetector(cd) end
                    
                    local center = camera.ViewportSize / 2
                    VIM:SendMouseButtonEvent(center.X, center.Y + 36, 0, true, game, 1)
                    task.wait(0.01)
                    VIM:SendMouseButtonEvent(center.X, center.Y + 36, 0, false, game, 1)
                    
                    VIM:SendKeyEvent(true, Enum.KeyCode.E, false, game)
                    task.wait(0.05)
                end
                camLock:Disconnect()
                hrp.Anchored = false
            end
        end
    end

    -- First pass
    collectAll(items)
    
    -- MODIFIED: Final re-check before hopping
    task.wait(0.1)
    local remaining = performScan()
    if #remaining > 0 then
        collectAll(remaining)
    end
    
    serverHop()
end

task.spawn(runTurbo)
